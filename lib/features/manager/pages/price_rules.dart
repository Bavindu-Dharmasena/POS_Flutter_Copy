import 'package:flutter/material.dart';

class PriceRulesPage extends StatelessWidget {
  const PriceRulesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Price Rules')),
      body: const Center(child: Text('TODO: Configure discounts / markups / time-bound rules')),
    );
  }
}
