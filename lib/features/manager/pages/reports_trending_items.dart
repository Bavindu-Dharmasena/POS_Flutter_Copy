import 'package:flutter/material.dart';

class TrendingItemsReportPage extends StatelessWidget {
  const TrendingItemsReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reports • Trending Items')),
      body: const Center(child: Text('TODO: Show top-selling items over selected period')),
    );
  }
}
