name: pos_system
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2 # Optional: upgrade, 6.1.1 still works
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  lottie: ^3.3.1
  go_router: ^16.1.0
  flutter_colorpicker: ^1.0.3
  google_fonts: ^6.2.1
  flutter_vector_icons: ^2.0.0
  mobile_scanner: ^7.0.1
  shared_preferences: ^2.0.15 # ✅ Needed for theme/font size persistence
  http: ^1.5.0
  flutter_dotenv: ^5.0.2
  flutter_secure_storage: ^8.0.0
  dio: ^4.0.0
  cookie_jar: ^3.0.1
  dio_cookie_manager: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  provider: ^6.1.0
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env
